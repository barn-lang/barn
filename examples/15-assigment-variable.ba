fun pow2(float x) -> float {
    return (x * x)
}

fun main() -> int {
    let pi: float = 3.15
    pi = 3.14 // Whoops my bad
    let circle: float = 0
    circle = pi * (20*20)

    return 0 // Success
}