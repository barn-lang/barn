@import "std.ba"

fun pow2(float x) -> float {
    return (x * x)
}

fun main() -> int {
    let pi: float = 3.15
    pi = 3.14 // Whoops my bad

    let area: float = pi * pow2(pi)
    fmt_print("area: %f", area)
    return 0 // Success
}