fun function_with_return() -> int {
    let lol: int = 30
    lol += 10
    lol -= 5
    lol *= 2
    lol /= 2
    return 10 + 20 + lol
}

fun get_name(string name) -> string {
    return name
}

fun main() {
    let name: string = get_name("Bruno")
    // __code__("name = get_name(\"Bruno\");")
    printf(name)
}